extends layout

block content
  h1 Admin Dashboard
  form(name="admin-survey", action="/login", method="post")
        div.input
            span.label Username: 
            input(type="text", name="username")
        br
        div.input
            span.label Password: 
            input(type="text", name="password")
        br
        div.actions
            input(type="submit", value="Login")
        